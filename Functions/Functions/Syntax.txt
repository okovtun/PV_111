type name(parameters)
{
	....;
	group-of-statements;
	....;
}
type - тип возвращаемого значения.
	   bool, char, int, double....
	   void - пустота.
	   return value;
return - прерывает работу функции и возвращает значение на место вызова.
return так же возвращает управление на место вызова функции.
ключевое слово return может вернуть только одно значение, 
либо вернуть только управление, без каких-либо значений.

name - имя функции.

parameters - это список принимаемых параметров

Unresolved external symbol found....