Array - это набор переменных одного типа в непрерывной области пам€ти.

					type name[SIZE];
type - тип элементов массива. Ёлементы массива могут быть любого 
	   существующео типа: bool, char, int, float....
name - им€ массива. ƒл€ именовани€ массивов используютс€ такие же 
	   идентификаторы, как и дл€ именовани€ переменных.
SIZE - количество элементов массива. ћожет быть задано только 
	   целочисленным константным значением.

	   const int SIZE = 5;	//объ€вл€ем размер массива
	   int arr[SIZE];		//объ€вл€ем сам массив

	   int arr[SIZE] = { 3, 5, 8, 13, 21 };	//ќбъ€вление и инициализаци€ массива